# adapters

Future home for adapter implementations.

Expected modules:

- LLM adapters.
- Internal tool adapters.
- CLI agent adapters.

Keep provider-specific behavior here instead of mixing it into workflow logic.
